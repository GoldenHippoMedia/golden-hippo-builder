---
'@goldenhippo/builder-cart-schemas': minor
---

Add `applicationType` select field (exclusive | preferred | required, defaults to preferred) to the `profile-reference-rule` model, exposed via the new `ProfileReferenceApplicationType` enum.
