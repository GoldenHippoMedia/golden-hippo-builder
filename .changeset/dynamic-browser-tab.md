---
'@goldenhippo/builder-cart-schemas': minor
---

Add `dynamicBrowserTab` config to the brand-config features section, controlling the blinking browser tab title shown when a user switches away from the tab (`enabled`, `awayTitle`, optional `defaultTitle`).
