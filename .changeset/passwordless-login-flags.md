---
'@goldenhippo/builder-cart-schemas': minor
---

Add passwordless login feature flags to brand config: `passwordlessLoginEnabled` toggles passwordless login for the brand, and `passwordlessLoginDefault` presents it as the default login method (only shown when passwordless login is enabled).
