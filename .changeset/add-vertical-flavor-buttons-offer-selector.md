---
'@goldenhippo/builder-cart-schemas': patch
---

Add `Vertical - Flavor Buttons - Type Toggle` to the `OfferSelectorTypes` enum and expose it as an option on the page model's PDP offer selector `osType` dropdown.
