---
'@goldenhippo/builder-cart-schemas': minor
'@goldenhippo/builder-cart-plugin': minor
---

Add `payment-config` data model referenced from `gh-brand-config`, with a `payPalGuestEnabled` flag that controls whether the PayPal checkout button is shown for guest users on the cart page.
