---
'@goldenhippo/builder-shared-schemas': patch
---

Add `faqs` field to the product model — an optional object containing a list of FAQ items (`faqItem`), each with a `faqTitle` (text) and `faqDescription` (HTML). Used to render an FAQ accordion on the product detail page.
