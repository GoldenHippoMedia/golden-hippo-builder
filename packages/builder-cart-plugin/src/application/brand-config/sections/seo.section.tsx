import React from 'react';
import { SectionProps } from './section-props';

const SeoSection: React.FC<SectionProps> = () => {
  return <p className="text-base-content/60">SEO section — coming soon</p>;
};

export default SeoSection;
