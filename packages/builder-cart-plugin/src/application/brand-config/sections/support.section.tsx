import React from 'react';
import { SectionProps } from './section-props';

const SupportSection: React.FC<SectionProps> = () => {
  return <p className="text-base-content/60">Support section — coming soon</p>;
};

export default SupportSection;
