import React from 'react';
import { SectionProps } from './section-props';

const CookieSection: React.FC<SectionProps> = () => {
  return <p className="text-base-content/60">Cookie section — coming soon</p>;
};

export default CookieSection;
